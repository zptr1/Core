^ io;

0 main {
  2 == 2 ? {
    io:puts("the universe works properly");
  } : {
    io:puts("oh no, the end of the world has happened!");
  }
  
  io:puts(1 ? "ok" : "not ok");

  32 a = 10;
  32 b = 5;
  a > b ? io:puts("a is bigger than b");
}